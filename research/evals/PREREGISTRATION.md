# Preregistration

Complete and commit this file before the main run.

- Frozen task-manifest commit:
- Frozen `models.lock.json` hash:
- Pilot task IDs:
- Main task IDs:
- Primary comparison: `kit_plus_hyper` versus `native_raw`
- Secondary comparisons:
- Exclusion rules fixed before run:
- Paired bootstrap seed:
- Reviewer identities and blinding procedure:
- Amendment history:

No task, condition, metric, or exclusion rule may be changed after the main run
starts without a dated amendment that preserves the original analysis.
